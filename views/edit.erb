<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Document</title>
</head>
<body>
  <div class="container">
    <form action="/menus/<%= @menu.id %>" method="post">
      <div class="controls">
        <div class="form-group">
          <p>料理名</p>
          <input class="form-control input-lg" type="text" name="title" placeholder="料理名はなに？" value="<%= CGI.escapeHTML(@menu.title) %>"><br>
          <p>材料</p>
          <input class="form-control input-lg" type="text" name="material" placeholder="材料はなに？" value="<%= @menu.material %>"><br>
          <p>作り方</p>
          <input class="form-control input-lg" type="text" name="howto" placeholder="どうやってくつるの？" value="<%= @menu.howto %>"><br>
          <p>かかる時間</p>
          <select class="form-control" name="list">
            <% List.all.each do |list| %>
              <option value="<%= list.id %>"
                <% if list.id == @menu.list.id %>
                  selected
                <% end %>>
                <%= list.name %>
              </option>
            <% end %>
          </select>
        </div>
        <div class="form-group col-lg-4">
          <input class="form-control col-lg-4" type="submit" value="登録">
        </div>
      </div>
    </form>
  </div>
</body>
</html>